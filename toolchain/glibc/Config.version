if USE_GLIBC

config GLIBC_VERSION
	string
	default "2.24" if GLIBC_VERSION_2_24
	default "2.27" if GLIBC_VERSION_2_27

config GLIBC_VERSION_2_24
	bool

config GLIBC_VERSION_2_27
	default y if !TOOLCHAINOPTS
	bool

endif
